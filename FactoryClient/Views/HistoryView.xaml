<UserControl x:Class="FactoryClient.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="220"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#F8FAFC"
                BorderBrush="#D6DCE8"
                BorderThickness="1"
                CornerRadius="10"
                Padding="12"
                Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="시작일" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <DatePicker SelectedDate="{Binding FromDate}" Width="140"/>

                <TextBlock Text="종료일" VerticalAlignment="Center" Margin="16,0,8,0"/>
                <DatePicker SelectedDate="{Binding ToDate}" Width="140"/>

                <Button Content="조회"
                        Width="100"
                        Height="34"
                        Margin="16,0,0,0"
                        Command="{Binding SearchCommand}"/>

                <TextBlock Text="{Binding StatusMessage}"
                           Margin="20,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="#475569"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="HistoryGrid"
                  Grid.Row="1"
                  ItemsSource="{Binding Events}"
                  SelectedItem="{Binding SelectedEvent}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="HistoryGrid_SelectionChanged"
                  Margin="0,0,0,12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                <DataGridTextColumn Header="카메라" Binding="{Binding CameraName}" Width="120"/>
                <DataGridTextColumn Header="제품명" Binding="{Binding ProductName}" Width="150"/>
                <DataGridTextColumn Header="발생시간" Binding="{Binding EventTime}" Width="180"/>
                <DataGridTextColumn Header="생산카운트" Binding="{Binding ProductionCount}" Width="100"/>
                <DataGridTextColumn Header="이미지경로" Binding="{Binding ImagePath}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2"
                Background="#F8FAFC"
                BorderBrush="#D6DCE8"
                BorderThickness="1"
                CornerRadius="10"
                Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="이미지 미리보기"
                           FontSize="16"
                           FontWeight="Bold"
                           VerticalAlignment="Top"/>

                <Border Grid.Column="1"
                        BorderBrush="#CBD5E1"
                        BorderThickness="1"
                        Background="White">
                    <Image x:Name="PreviewImage" Stretch="Uniform"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>